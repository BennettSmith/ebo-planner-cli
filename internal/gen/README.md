# `internal/gen`

This directory contains **generated** code.

- Generated from the pinned spec reference in `spec.lock`.
- Regenerate with: `make gen`

Notes:

- Generated code is excluded from the repo’s coverage gate (see `Makefile`).
