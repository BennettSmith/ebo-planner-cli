# `internal/adapters/out`

Outbound adapters implementing `internal/ports/out`.

This is where IO lives:

- network (OpenAPI client wrapper)
- filesystem (config persistence)
- environment/OS integration
